# Core LangChain
langchain>=0.3.0
langchain-core>=0.3.0

# LLM provider packages (install the one(s) you need)
langchain-anthropic>=0.3.0
langchain-openai>=0.2.0
langchain-openrouter>=0.1.0

# JSON schema validation for generated node docs
check-jsonschema>=0.28.0

python-dotenv

# Observability / tracing (optional – set LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY)
langfuse>=2.0.0

# Schema
check-jsonschema
